package fovis;

struct stats_t {
    int64_t timestamp;
    int32_t num_matches;
    int32_t num_inliers;
    int32_t num_detected_keypoints;
    int32_t num_reprojection_failures;
    int32_t num_keypoints; // after pruning
    double mean_reprojection_error;
    double fast_threshold;
}
